# Example Title

## Purpose

说明本 example 展示什么场景，以及它为什么属于 Examples 模块。

本节只定义示例目标，不新增投资原则。

## Input

说明示例输入。

输入必须标记为示例，不应被视为实时市场事实或投资建议。

```text
示例输入：

- 日期或周期：
- 投资者背景：
- 当前问题：
- 已知事实：
- 信息缺口：
```

## Prompt Used

说明本 example 使用哪个已经存在的 prompt。

```text
Prompt:

prompts/<prompt-file-name>.md
```

不得在 example 中改写 prompt 规则、输出结构或责任边界。

## Expected Output

说明预期输出应具备的结构和判断方式。

本节只描述输出应该如何符合既有 prompt，不生成新的 prompt 功能。

```text
预期输出应包括：

- 结论：
- 关键判断：
- 信息缺口：
- 后续事项：
```

## IOS Principles Applied

列出本 example 使用到的 IOS 原则。

只允许引用 Book 中已经存在的原则，例如：

- 企业质量高于价格；
- 产业决定公司，公司决定股价；
- 资本配置比选股更重要；
- 默认长期持有优秀企业；
- 新 NISA 应服务长期免税复利；
- 估值必须建立在企业质量之后。

## Why

解释为什么这个 example 符合 IOS 和 Prompt Suite 边界。

必须说明：

- 为什么该输入适合使用这个 prompt；
- 为什么该输出不构成交易建议；
- 为什么没有新增投资规则；
- 为什么没有扩大 prompt 责任范围。

## Related Documents

列出关联文件。

```text
Book:

- book/<chapter>.md

Prompt:

- prompts/<prompt-file-name>.md

Template:

- templates/<template-file-name>.md
```

如果没有关联模板，应写：

```text
Template:

- 无
```
